# Vale configuration — https://vale.sh/docs/topics/config
StylesPath = .vale/styles
MinAlertLevel = suggestion

# Downloaded by `vale sync` (the GitHub Action does this automatically).
Packages = proselint

[*.md]
BasedOnStyles = Vale, proselint
